# Local ssh configuration for this machine.
#
# Included from the top of ~/.ssh/config. ssh uses the first value it obtains
# for each option, so entries here take precedence over the committed ones -
# which is exactly why the include sits at the top rather than the bottom.
#
# Not tracked in the dotfiles repo and never overwritten by the installer.
